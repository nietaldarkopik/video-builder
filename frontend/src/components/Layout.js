import Header from "./Header";
import MainContainer from "./MainContainer";

const Layout = () => {
    return (
        <>
            <Header />
            <MainContainer />
        </>
    )
};

export default Layout;